<h1> Group Notices</h1>
<% @notices.each do |notice| %>
<div class="col-md-4">
<div class="message">
<h2 class="message-title"><%= notice.title %></h2>
<%= link_to "View Notice", notice_path(notice), class: "btn btn-custom" %>
</div>
</div>
<% end %>